---
import List from '../../components/list.astro'
import { getCursorPageChannel, noIndexSeo } from '../../lib/pagination'

const channel = await getCursorPageChannel(Astro, 'before')
---

<List channel={channel} pageHeading={channel.title} pageSeo={noIndexSeo} />
